- Search for sorting source files in a better way than in a single directory

- Find the bug that cause the character to move when it is in the middle of the
  scrolling rectangle because it shouldn't

- Code a level editor
